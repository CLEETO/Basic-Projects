This is a model of a shop billing system. It uses a mysql database to store data. It can be executes either by makeing a .jar file using the project file or by executing the class named "one". Necessary changes have to be made in the class named "connect" so that proper connection to the mysql database in your system.
